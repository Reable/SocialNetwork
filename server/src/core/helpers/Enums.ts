export enum UserRole {
    USER_ROLE = "user",
    ADMIN_ROLE = "admin",
    DEVELOPER_ROLE = "developer"
}

export enum HEADERS {
    AuthTokenName = "authorization"
}

export enum DB_USER {
    USER_TABLE_NAME = "users"
}

export enum DB_MESSAGE {
    CHATS = "chats",
    CHAT_MEMBERS = "chats_members",
    PRIVATE_CHAT = "private_chats",
    PRIVATE_MESSAGES = "private_messages"
}